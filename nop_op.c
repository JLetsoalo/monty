#include "monty.h"

/**
 * nop - doesn't take any action
 * @h: head of stack
 * @line_number: current line_number
 */
void nop(stack_t **h, unsigned int line_number)
{
	(void) line_number;
	(void) h;
}
